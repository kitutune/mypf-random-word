import type { NextPage } from 'next';
import { Login } from 'components/Login';
const Home: NextPage = () => {
  return (
    <main>
      amu
      <Login />
      last
    </main>
  );
};

export default Home;
