import type { NextPage } from 'next';
import { RandomWord } from 'components/RandomWord';

const Home: NextPage = () => {
  return (
    <main>
      amu
      <div>
        <RandomWord />
      </div>
      last
    </main>
  );
};

export default Home;
